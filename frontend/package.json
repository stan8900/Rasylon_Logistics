{
  "name": "rasylon-logistics-miniapp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "start": "vite preview --host 0.0.0.0 --port ${PORT:-4173}"
  },
  "dependencies": {
    "vite": "^6.4.3"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
